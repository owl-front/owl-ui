export enum ShapeType {
  Default = "default",
  Circle = "circle",
  Round = "round",
}

export enum Size {
  Large = "large",
  Middle = "middle",
  Small = "small",
}

export enum Type {
  Primary = "primary",
  Dashed = "dashed",
  Link = "link",
  Text = "text",
  Default = "default",
}
